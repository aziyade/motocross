{% extends 'base.html.twig' %}

{% block title %}Ajouter Event{% endblock %}

{% block body %}
  <div class="container">
    <div class="row">
      <!-- 100% 12 colonnes -->

      <div class="offset-2 col-8">
        <h3>Ajouter un évènement</h3>
        {# <form ...>  #}
        {{ form_start(form) }}
        {# afficher tous les champs #}
        {{ form_widget(form) }}    
        <button type="submit" class="btn btn-primary">
          <i class="fas fa-edit"></i>
          Ajouter
        </button>
        <br><br>
        {# </form>  #}
        {{ form_end(form) }}
      </div>
    </div>
  </div>
{% endblock %}
